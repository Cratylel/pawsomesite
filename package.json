{
	"name": "pawsomesite",
	"description": "a minimal server-rendered site running on cloudflare workers",
	"private": true,
	"dependencies": {
		"hono": "^4.13.2"
	},
	"devDependencies": {
		"@types/node": "24.10.1",
		"typescript": "5.9.3",
		"wrangler": "4.123.0"
	},
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"build": "tsc",
		"types": "wrangler types",
		"check": "tsc && wrangler deploy --dry-run"
	}
}
